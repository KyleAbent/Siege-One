
/*
function Rifle:GetClipSize()
    
           local player = self:GetParent()
        if player then
        return ConditionalValue( GetHasTech(player, kTechId.ClipSize1), kRifleClipSize * 1.10, kRifleClipSize)  
        else
        return kRifleClipSize
        end
   
end
*/